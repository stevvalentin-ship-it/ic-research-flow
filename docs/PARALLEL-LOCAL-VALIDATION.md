# 双 PDF 接入：本机验收记录

日期：2026-09-14。运行目标：`http://127.0.0.1:5174` 的 IC Research Flow 网页。

## 最终从头运行

最终证据目录：`outputs/parallel-2026-09-14T12-08-37-307Z/`。此前的试跑目录不作为本次验收结论。

- 论文：cuZK，原 PDF 共 26 页。
- 原文件 SHA-256：`b704560bdbec7064c5cec455ed65ecb5555974c0d96a0c8a5dd14747b39baed6`。
- 使用全新浏览器配置，并在网页勾选清空本篇缓存。开始前 IC 与新核心数据库的所有表均为 0，见 `empty-storage.json`。
- 通过网页导入、配置模型、确认发送范围及启动翻译。真实 DeepSeek 请求 91 次，全部使用 `deepseek-flash`，未拦截或模拟模型响应。
- 完成 283 个翻译文本块，生成 21 页中文 PDF，任务耗时约 349 秒。最终事件记录没有前次任务缓存命中。
- 中文 PDF SHA-256：`71d7ab4e6ea081c073e7f8fb5f0a79c7a1a32a9b991d2542039799b22d5c956c`。

## 实际网页检查

- 21 页中文 PDF 全部逐页渲染成功。
- 点击原文第 6 页的 Pippenger 标题，自动跳至中文第 5 页；反向点击中文可回到英文第 6 页，两侧出现对应高亮。
- 开启段落同步后按内容定位；关闭后可独立滚动、独立翻页。
- 原文哈希保持不变，校对页可读取第 1 页的 8 条真实译文记录。
- 中文 PDF 和检查报告均从网页下载；下载报告与本次保存的 PDF 输出报告一致。
- 刷新后原文与译文仍可打开；验收过程没有浏览器脚本错误。
- 实际点击检查了阅读页整篇翻译、校对页重排、校对页继续翻译、论文库菜单、研究台菜单五个入口。它们都打开同一新核心设置流程；此入口检查没有另行提交收费任务。
- 任务恢复使用同一翻译入口；`/translate`、`/compose` 共用核心的连接另有自动测试覆盖。

详细记录见 `verification.json`、`entry-points.json`、`api-requests.json`、`engine-events.json`；界面见 `reader-final.png`，抽查页面见 `target-page-*.png`。

## 质量结论与边界

本次按用户要求采用“优先生成可读版本”，已完成可阅读、定位、校对和下载流程。它不是严格审校通过的成品：逐页模型报告共 31 项问题，主要是缩进、断行等；第 9 页有孤立英文尾片段，第 10 页仍有英文碎片。抽查确认正文、公式、图表大体可读，但上述问题仍需后续修补。

报告保留 `pass: false`、`deliveryStatus: needs-review`，阅读页明确显示“部分内容待复核”。不以放宽交付策略伪造质检通过。

工程检查：28 个测试文件、84 项测试通过；TypeScript 检查、生产构建及 `git diff --check` 通过。变更范围和旧项目备份格式的边界见 `browser/parallel/README.md`。
